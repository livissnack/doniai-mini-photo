var formatTime = function (time) {
  console.log(time)
  var d = getDate(time)
  console.log(d)
  return d.getFullYear() + '年'+ (d.getMonth()+1) + '月' + d.getDate() + '日'
}

module.exports = {
  formatTime: formatTime,
}